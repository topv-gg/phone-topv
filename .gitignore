ui/node_modules/
ui/.env
ui/.env.*

# Auto-generated TopV key (one secret per server, never in git). From 1.3.0 it
# lives inside this folder. Both paths are listed: the old one, one level up,
# still exists on servers updating from 1.2.0 or earlier.
topv_autokey.json
../topv-autokey.json

# Working backups: never versioned, and never left in the folder shipped to
# server owners.
*.bak
*.bak-*

